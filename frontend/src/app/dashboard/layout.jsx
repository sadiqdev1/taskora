export const metadata = {
  title: 'Dashboard — Taskora',
  description: 'Your Taskora earnings dashboard. Track wallet balance, active campaigns, completed tasks, and recent earnings.',
  robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
  return children;
}
