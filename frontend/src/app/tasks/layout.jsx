export const metadata = {
  title: 'My Tasks — Taskora',
  description: 'Track all your task submissions, approvals, and earnings on Taskora.',
  robots: { index: false },
};

export default function TasksLayout({ children }) {
  return children;
}
