export const metadata = {
  title: 'Admin Panel — Taskora',
  description: 'Taskora admin dashboard — manage campaigns, users, submissions and withdrawals.',
  robots: { index: false, follow: false },
};

export default function AdminLayout({ children }) {
  return children;
}
